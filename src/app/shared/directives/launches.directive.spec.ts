import { LaunchesDirective } from './launches.directive';

describe('LaunchesDirective', () => {
  it('should create an instance', () => {
    // const directive = new LaunchesDirective();
    // expect(directive).toBeTruthy();
  });
});
